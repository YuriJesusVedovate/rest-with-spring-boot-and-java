server:
  port: 8888
  servlet:
    context-path: /api
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show_sql: true
    open-in-view: true
spring-doc:
  paths-to-match: /api/**
  swagger-ui:
    use-root-path: true